串口读写相关

例子：
1，PC用一条串口线与ipc232相连。PC上运行一个程序，监听串口ttyS0，ipc232板上运行读写程序。







读写：
    往串口写入N个数据段，如果开发板对每个数据段回应M字节，那么，开发板会回应N*M个字节。
    
    这N*M个字节，可以分为N次，每次M个字节，也可以分为一次，直接完全接收N*M个字节。
    
    读写端就像一个文件一样，读写的时候，存储在文件，读写时，也有读写指针。所以，那N*M个字节，如果不读取时，仍旧存储着，一旦读取，就是从N*M个字节开始，如果想忽略这N*M个字节，则需要读取出来，只是不处理。
